<% unless current_user == @user %>
  <div id="favorite_form">
    <% if current_user.favorited?(@song) %>
      <%= render "unfavorite" %>
    <% else %>
      <%= render "favorite" %>
    <% end %>
  </div>
<% end %>
