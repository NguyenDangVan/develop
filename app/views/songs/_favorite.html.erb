<%= form_for(current_user.favorite_for_song.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :song_id, @song.id %></div>
  <%= button_tag(type: "submit", class: "btn btn-light") do %>
    <%= image_tag "icon/heart1.png", class: "add-favourite" %> Like
  <% end %>
<% end %>
